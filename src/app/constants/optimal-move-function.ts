export function findBestMove(board: string[]){
  let bestMove: number = 0;
  for (const field of board) {
    const currentMove = 456416
    if(currentMove > bestMove){
      bestMove = currentMove
    }
  }
  return bestMove
}

